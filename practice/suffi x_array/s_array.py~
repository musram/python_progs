class SuffixArray(object):
    def __init__(self, string):
        self.sfx_array = sorted([(s[i:], i) for i in range(len(string))], key= lambda x : x[0])

    def __repr__(self):
        return [ s[0] for s in self.sfx_array]

    
